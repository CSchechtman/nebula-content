/* 
Copy the following text to your notes, then create variables and assign them strings that will help the bottom console.log run properly:

Write your code below the comment and above the console log 
*/

const language = "javascript"
const adjective1 = "enjoy"
const keyword1 = "var"
const year = "1842"
const instructor = "Eric"

console.log(
    `I am currently learning the programming language called ${language}. I ${adjective1} this program already. I read recently that declaring variables with the ${keyword1} keyword has gone out of style ever since ECMAScript ${year}. Oh, also, my instructor ${instructor} is awesome!`
  );

  // Using only the alphabet variable below, bracket notation & concatenation, console.log out your first name
const alph = "abcdefghijklmnopqrstuvwxyz";
const letterC = alph[2]
const letterR = alph[17]
const letterA = alph[0]
const letterI = alph[8]
const letterG = alph[6]

console.log(alph[2] + alph[17] + alph[0] + alph[8] + alph[6]);
console.log(letterC + letterR + letterA + letterI + letterG);
